document.addEventListener('DOMContentLoaded', function () {
	var rules = {};
	var rows = document.querySelectorAll('.row');
	rows.forEach((row) => {
		var variable = row.querySelector('.variable');
		var values = row.querySelectorAll('.value');
	});
});
